spring:
   boot:
    admin:
      client:
        url: http://localhost:2126

management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  port: 4200


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true


msg: "Welcoom to Our Greet Api"
